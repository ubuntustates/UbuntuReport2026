# global_emergency_news/docker-compose.yml

version: "3.9"

services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    volumes:
      - .:/app
    command: npm run dev
